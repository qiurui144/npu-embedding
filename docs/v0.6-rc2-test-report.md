# v0.6.0-rc.2 全面测试报告

> 测试时间: 2026-04-27
> 测试范围: attune (OSS) + attune-pro (Pro) 双仓 + Extension + Python 原型线 + GitHub Actions
> 测试目的: 验证 v0.6.0-rc.2 OSS 边界瘦身（per oss-pro-strategy v2 三产品矩阵）后系统完整性
>
> **总评级**: ⏳ **Ready for v0.6.0-rc.2** — 1 已知非阻塞问题（Build Windows Installer 是 Python 原型线遗留 noise，不影响 Tauri 桌面 release）

---

## 测试金字塔层次

```
                    ┌─────────────────┐
                    │ L6 CI/CD 状态    │  ⏳ rc.2 release pipeline
                    ├─────────────────┤
                    │ L5 质量回归      │  ✅ K2 + RAGAS framework
                    ├─────────────────┤
                    │ L4 e2e + 烟测    │  ✅ plugin loading + server up
                    ├─────────────────┤
                    │ L3 跨语言扩展     │  ✅ Extension / ⚠️ Python (本地)
                    ├─────────────────┤
                    │ L2 lint + 边界    │  ✅ 0 残留律师术语 production
                    ├─────────────────┤
                    │ L1 单元 + 集成   │  ✅ attune 29 + Pro 9 全绿
                    └─────────────────┘
```

---

## L1: Rust 双仓单元 + 集成测试

### L1.1 attune (OSS) — `cargo test --release --workspace`

| 维度 | 数据 |
|------|------|
| 测试套件 | **29 suites** |
| 总测试数 | ~466 (440 lib + 集成测试) |
| 通过 | **全部** ✅ |
| 失败 | **0** |
| ignored | 1 (rag_quality_benchmark::run_benchmark_against_real_corpus 真 corpus，手工触发) |

**关键集成测试**：
- ✅ `attune-core::lib` ~440 单元测试
- ✅ `attune-server::lib` 含 W4-005 routes::errors 5 测试
- ✅ `parse_golden_set_regression` 8 测试 (W3 batch C K2)
- ✅ `rag_quality_benchmark` 7 测试 (W4 J6 RAGAS deterministic)
- ✅ `rag_w2_batch1_integration` 11 测试 (J1/J3/J5 + B1)
- ✅ `rag_w3_batch_a_integration` 10+ 测试 (F2/C1/F1/F4)
- ✅ `rag_w3_batch_b_integration` 5 测试 (G1/G5/F3)
- ✅ `migration_roundtrip_test` 4 测试 (W4-004 持久化文件)
- ✅ `governor_integration` (H1)
- ✅ `memory_consolidation_integration` (A1)
- ✅ `entities_test` 6 测试 (Phase A 通用化后)
- ✅ `project_recommender_test` 5 测试 (Phase A keywords plugin 化后)

### L1.2 attune-pro — `cargo test --release --workspace`

| 维度 | 数据 |
|------|------|
| Workspace member | 4 (law-pro / presales-pro / patent-pro / tech-pro) |
| 测试套件 | **9 suites** |
| 通过 | **全部** ✅ |
| 失败 | **0** |

**关键测试**：
- ✅ `law-pro lib` 2 测试 (case_no extractor 烟测 + version)
- ✅ `law-pro extractors::case_no` 3 测试 (中文法律案号正则)
- ✅ `law-pro tests::integration_loading_test` **4 测试** ⭐ (e2e plugin 装载链路)
- ✅ `patent-pro / tech-pro / presales-pro lib` 各 1 测试 (placeholder)

---

## L2: Lint + OSS 边界审计

### L2.1 OSS production code 残留律师术语 grep

```bash
grep -rn "case_no\|案件\|起诉\|诉讼\|律师\|EntityKind::CaseNo" \
  rust/crates/attune-core/src/ rust/crates/attune-server/src/ \
  | grep -v "^//\|tests::\|test_"
```

**结果**: **0 行** production logic 含律师专属术语 ✅

发现的注释引用（合规边界声明，非违规）：
- `plugin_sig.rs:5` 注释「商业插件（律师 / 售前 / 医疗等）通过 PluginHub 分发」— 边界说明 ✅
- `entities.rs:11/112-113` 注释 + Organization 正则含"律师事务所"作为通用机构后缀（与"医院/大学/研究所"同列）— 通用 ✅
- `project_recommender.rs:11/16` 注释说明历史变更（"之前 hardcode → 现在 plugin 聚合"）— 历史记录 ✅

测试 fixture 中的 1 处："chunk_breadcrumbs.rs:325 测试用 `# 案件分析` 内容" — 测试代码非 production，但建议改通用（W5 followup）。

### L2.2 OSS builtin 行业 yaml 验证

```bash
ls rust/crates/attune-core/assets/plugins/
```

**结果**: 仅 4 个 `ai_annotation_*` plugin (AI 批注角度，通用功能)，**0 个行业 yaml** ✅
- ❌ 已删: law.yaml / presales.yaml / patent.yaml / tech.yaml
- ✅ 保留: ai_annotation_highlights / ai_annotation_outdated / ai_annotation_questions / ai_annotation_risk

### L2.3 hardcoded chat trigger keywords

```bash
grep "CHAT_TRIGGER_KEYWORDS\|=.\"案" rust/crates/attune-core/src/ -r
```

**结果**: **0 行** ✅ — const 已删除，改为 PluginRegistry 聚合

---

## L3: 跨语言扩展

### L3.1 Python 原型线 — `pytest tests/`

**状态**: ⚠️ 本地缺 venv (PEP 668 阻断 system pip install)

CI 上正常跑（`.github/workflows/ci.yml::lint-and-test::Test`），develop branch 最新 push (commit `887ef31`) 触发 CI 已 success（per L6 状态）。

本地复现需建 venv：
```bash
python3 -m venv venv && source venv/bin/activate
pip install -e ".[dev]"
pytest tests/ --ignore=tests/test_extension.py
```

### L3.2 Chrome 扩展构建 — `cd extension && npm run build`

**结果**: ✅ 4 个 vite build 全部 success（80-130ms 每个）

产出（per CI verify step）：
- ✅ `dist/content/index.js`
- ✅ `dist/content/browse_capture.js` (G1 通用浏览捕获)
- ✅ `dist/content/indicator.css`
- ✅ `dist/popup/index.html`
- ✅ `dist/sidepanel/index.html`
- ✅ `dist/options/index.html`
- ✅ `dist/background/worker.js`

---

## L4: e2e plugin 装载 + 服务端烟测

### L4.1 attune-pro 集成测试

`law-pro::tests::integration_loading_test` **4/4 全绿** ✅

| 测试 | 验证内容 |
|------|---------|
| `scan_loads_all_four_vertical_packs` | PluginRegistry::scan 扫到 law/presales/patent/tech 4 plugin |
| `aggregated_chat_trigger_project_keywords_match_expected` | 跨 plugin keywords 聚合 + 去重（"项目" 共享词只 1 次） |
| `recommend_for_chat_with_pro_keywords_triggers_correctly` | 真触发: law"案件" / presales"RFP" / patent"权利要求" |
| `empty_plugins_dir_simulates_oss_only_install` | OSS 边界硬证: 空 dir → 律师术语永不触发 |

### L4.2 attune-server-headless 启动烟测

```
[INFO attune_server::state] loaded 16 plugins, 0 workflows from /home/qiurui/.local/share/attune/plugins
[INFO attune_server] attune-server listening on http://127.0.0.1:18900
```

**16 plugins 加载明细**:
- 4 attune-pro vertical packs (law/presales/patent/tech) 顶层
- 5 law-pro capabilities 子目录（contract_review / clause_lookup / drafting / oa_reply / risk_matrix）
- 4 presales-pro capabilities 子目录（competitive_analysis / bant / poc_proposal / quote）
- 3 历史 ai_annotation_* + rust_helper 测试 plugin

HTTP 健康检查：
```
$ curl -s http://localhost:18900/api/v1/status
{"error":"missing bearer token"}
```
✅ 服务端响应正常（401 风格鉴权要求是预期行为，per CLAUDE.md require_auth）

---

## L5: 质量回归

### L5.1 K2 Parse Golden Set Regression — W3 batch C

```
running 8 tests
test k2_001_rust_ownership_passes ... ok
test k2_002_china_civil_code_passes ... ok
test k2_003_tech_blog_passes ... ok
test k2_004_news_article_passes ... ok
test k2_005_academic_paper_passes ... ok
test k2_manifest_loads ... ok
test k2_all_fixtures_exist ... ok
test k2_baseline_corpus_passes_min_rate ... ok

test result: ok. 8 passed; 0 failed
```

**验证**:
- ✅ 5 fixture（rust ownership / 民法典 / 技术博客 / 新闻 / 学术论文）全 chunker pass
- ✅ `min_pass_rate = 1.0` 严格门槛通过
- ✅ Manifest 加载 + 文件存在性检查 + 整体 baseline 通过率验证

### L5.2 RAGAS Quality Benchmark Framework — W4 J6

```
running 8 tests
test aggregate_handles_empty_query_set ... ok
test aggregate_three_queries_fixed_baseline ... ok
test hit_at_k_returns_one_when_first_is_match ... ok
test hit_at_k_zero_when_no_match_in_topk ... ok
test golden_queries_file_loads ... ok
test mrr_returns_inverse_of_first_hit_rank ... ok
test recall_at_k_partial_coverage ... ok
test run_benchmark_against_real_corpus ... ignored

test result: ok. 7 passed; 0 failed; 1 ignored
```

**验证**:
- ✅ Hit@K / Recall@K / MRR 算法实现正确（fixed-input 验证）
- ✅ Aggregate 三 query baseline 数学正确
- ✅ Golden queries.json schema 加载 sanity
- ⏳ Real corpus benchmark `#[ignore]` 留 v0.6.0 GA 跑数（per W4 J6 设计）

---

## L6: CI/CD release pipeline 状态

### v0.6.0-rc.2 tag 触发的 builds

| Workflow | 状态 | 备注 |
|----------|------|------|
| `Build Linux AppImage` | ✅ success | Python 原型线 AppImage（含 OSS attune Rust 部分） |
| `Rust Release` | ⏳ queued | 5 平台 build (Linux x86_64/aarch64 / macOS x86_64/aarch64 / Windows MSVC)；GitHub Actions macOS runner 调度延迟 |
| `Build Windows Installer` | ❌ failure | Python 原型线 NSIS — `Unable to find makensis executable`（Windows runner PATH 不含 NSIS）。**与 v0.6.0-rc.1 同 noise，不阻塞 attune Rust 主线发版**（Tauri 桌面 MSI/NSIS 走 desktop-release.yml） |

### develop branch CI

最新 commit `887ef31` (Git push 授权扩展) 触发：
- ✅ CI: success
- ✅ rust-test: success（含 K2 parse golden gate）
- ✅ extension-build: success（含 R18 #2 dist verify + artifact upload）
- ✅ lint-and-test: success（pytest --ignore test_extension.py）

### desktop-release.yml (alpha.3)

✅ commit `desktop-v0.6.0-alpha.3` 已 success（Tauri Linux deb/AppImage + Windows MSI/NSIS 全部产出）— 这是用户实际能下载装的桌面包。

---

## v0.6.0-rc.2 vs v0.6.0-rc.1 关键差异

| 维度 | v0.6.0-rc.1 | v0.6.0-rc.2 | 改善 |
|------|-------------|-------------|------|
| OSS production code 律师术语 | 1 处 (CaseNo) | **0** | ✅ 边界完整 |
| OSS builtin 行业 plugin yaml | 4 (law/presales/patent/tech) | **0** | ✅ 三产品矩阵 |
| OSS hardcoded 律师 keyword const | 6 词 | **0** (plugin 注册) | ✅ 可扩展 |
| attune-pro plugin packs | 2 active | **4 active + 接收 OSS 内容** | ✅ vertical 覆盖 |
| 战略文档 | v1 thick OSS-core | **v2 三产品矩阵 + 准入规则 + 决策示例** | ✅ 决策性 |
| README 双语产品矩阵摘要 | 无 | **有** (链接 strategy v2) | ✅ 透明 |
| e2e 集成测试 | 无（仅 attune-pro lib 烟测） | **4 测试** (load + aggregate + trigger + OSS edge) | ✅ 行为验证 |

---

## 已知非阻塞问题（v0.7 followup）

| # | 问题 | 严重度 | 修复时机 |
|---|------|--------|---------|
| 1 | `build-windows.yml` Python NSIS noise | Low | v0.7：移到 workflow_dispatch only / 删除 |
| 2 | `chunk_breadcrumbs.rs:325` 测试 fixture 含"# 案件分析" | Low | v0.7：改通用"# 项目分析" |
| 3 | Rust Release 5 平台 macOS build queue 延迟 | Low | 排队完成（监控） |
| 4 | Python 端本地缺 venv 跑 pytest | Low | 本地建 venv（CI 已通过） |
| 5 | release.yml `0s failure` GitHub trigger 残影 | Low | 历史 cosmetic noise，无功能影响 |

---

## 综合评级

| 维度 | 评级 | 阻塞? |
|------|------|------|
| 代码质量（test pass + 0 retro） | ✅ A | 否 |
| 边界完整性（OSS 0 行业绑定） | ✅ A | 否 |
| 跨语言扩展（Ext build / Python） | ✅ A- | 否（CI 跑过） |
| e2e 链路（plugin 装载 → 触发） | ✅ A | 否 |
| 质量回归（K2 + RAGAS framework） | ✅ A | 否 |
| Release pipeline | ⏳ B+ | 否（Tauri 桌面已 success） |

**Final Recommendation**: ✅ **Ready for v0.6.0-rc.2 → v0.6.0 GA soak**

soak 7 天观察用户反馈，无重大问题后打 v0.6.0 GA tag 进入正式发布。

---

## 复现命令（供 v0.6.0 GA 前重跑）

```bash
# L1: 双仓 cargo test
cd /data/company/project/attune/rust && cargo test --release --workspace
cd /data/company/project/attune-pro && cargo test --release --workspace

# L2: 边界审计
grep -rn "case_no\|案件\|起诉\|诉讼\|EntityKind::CaseNo" \
  rust/crates/attune-core/src/ rust/crates/attune-server/src/ \
  | grep -v "^//\|tests::\|test_"
ls rust/crates/attune-core/assets/plugins/  # 应仅 ai_annotation_*

# L3: Extension build
cd /data/company/project/attune/extension && npm run build

# L4: e2e 集成测试
cd /data/company/project/attune-pro && \
  cargo test --release -p law-pro --test integration_loading_test

# L5: 质量回归
cd /data/company/project/attune/rust && \
  cargo test --release -p attune-core --test parse_golden_set_regression && \
  cargo test --release -p attune-core --test rag_quality_benchmark

# L4 server smoke (需要本地 plugin 装载)
mkdir -p ~/.local/share/attune/plugins/
for v in law-pro presales-pro patent-pro tech-pro; do
  cp -r /data/company/project/attune-pro/plugins/$v ~/.local/share/attune/plugins/
done
./target/release/attune-server-headless &
sleep 2
curl -s http://localhost:18900/api/v1/status  # 期望 401 missing bearer token
pkill -f attune-server-headless
```
